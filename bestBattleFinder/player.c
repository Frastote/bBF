#include "player.h"

void initialisation(Player player){
	int numUnit = 0, i;
	srand(time(NULL));
	player.budget = BUDGET;
	while (budget > LESS_EXPENSIVE_COST && player.army.limit!=LIMIT){
		selectUnit(player.army)
	}
}

